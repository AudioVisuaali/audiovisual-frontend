// import { selectVideoInformationDomain } from '../selectors';

describe('selectVideoInformationDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
