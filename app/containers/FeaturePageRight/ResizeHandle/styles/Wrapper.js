import styled from 'styled-components';

const Wrapper = styled.div({
  display: 'flex',
  alignItems: 'center',
  position: 'absolute',
  zIndex: 22,
  top: 0,
  bottom: 0,
  left: 0,
  width: 0,
});

export default Wrapper;
