import styled from 'styled-components';
import Button from 'components/Button';

const CenteringButton = styled(Button)`
  margin: 0 auto;
  display: block;
  font-size: 16px;
`;

export default CenteringButton;
