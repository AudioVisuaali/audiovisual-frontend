import styled from 'styled-components';

const CommentCode = styled.div`
  background-color: rgba(0, 0, 0, 0.15);
  padding: 4px;
  border-radius: 4px;
  display: inline;
`;

export default CommentCode;
