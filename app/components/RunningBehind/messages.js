/*
 * RunningBehind Messages
 *
 * This contains all the text for the RunningBehind component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RunningBehind';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Syncing',
  },
});
