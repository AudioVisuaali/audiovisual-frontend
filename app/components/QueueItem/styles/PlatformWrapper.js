import styled from 'styled-components';

const PlatformWrapper = styled.span`
  margin-right: 8px;
  display: inline;

  & svg {
    width: 24px;
    height: 24px;
  }
`;

export default PlatformWrapper;
