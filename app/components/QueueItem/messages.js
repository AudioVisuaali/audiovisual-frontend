/*
 * QueueItem Messages
 *
 * This contains all the text for the QueueItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.QueueItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the QueueItem component!',
  },
});
