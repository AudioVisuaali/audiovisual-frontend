/*
 * Controls Messages
 *
 * This contains all the text for the Controls component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Controls';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Controls component!',
  },
});
